都是输入函数
scanf输入字符串%S遇到空格或回车就结束输入，空格或回车就会留在缓存区里，
当输入非法内容则会将错误的内容卡在scanf的缓存区里面，如果有
while(1)
{
    scanf("%d",&a);
    printf("%d",a);
}
如果输入一个字符则会一直打印错误的数字；

getchar一次只能输入一个字符，可以输入空格和回车。
gets可以输入字符串读取到回车就会将回车符丢弃在末尾加上'\0'